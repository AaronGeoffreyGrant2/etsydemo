<div class="jumbotron">
  <% if user_signed_in? %>
     <h1>Draw something using my built in drawing app!</h1><br>
     <h2>Just click on "New Listing".</h2>
  <% else %>
    <h1>Design your own video game<br>
    characters and make money selling them!</h1>
    <h2>The only thing that you have to do is just signup</h2>
  <% end %>
</div>

<div class="digg_pagination">
    <div class="page_info">
      <%= page_entries_info @listings %>
    </div>
    <%= will_paginate @listings, :container => false %>
  </div>

<%= form_tag listings_path, method: :get do %>
  <%= text_field_tag :name, "" %>
  <%= submit_tag 'Click Here to Search and Click Again to View All Listings' %>
<% end %>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">
        <div class="thumbnail">
        <%= link_to image_tag(listing.image.url), listing %>
          <div class="caption">
            <h3><%= listing.name %></h3>
            <p><%= number_to_currency(listing.price) %></p>
            <p><%= "Sold by #{listing.user.name}" %></p>
          </div>
      </div>  
      </div>
    <% end %>
</div>
</div>

<br>

<% if user_signed_in? %>
<%= link_to 'New Listing', new_listing_path, class: "btn btn-link", data: { no_turbolink: true } %>
<% end %>

<div id="fb-root"></div>
<script>(function(d, s, id) {
  var js, fjs = d.getElementsByTagName(s)[0];
  if (d.getElementById(id)) return;
  js = d.createElement(s); js.id = id;
  js.src = 'https://connect.facebook.net/en_US/sdk.js#xfbml=1&version=v2.12&appId=945831498925242&autoLogAppEvents=1';
  fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>

<div class="fb-share-button" data-href="http://aarongeoffreygrantetsydemoapp.herokuapp.com/" data-layout="button" data-size="large" data-mobile-iframe="true"><a target="_blank" href="https://www.facebook.com/sharer/sharer.php?u=http%3A%2F%2Faarongeoffreygrantetsydemoapp.herokuapp.com%2F&amp;src=sdkpreparse" class="fb-xfbml-parse-ignore">Share</a></div>